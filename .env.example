# .env.example
#
# This file serves as a template for the .env file.
# Copy this file to .env and replace the placeholder values with your local development secrets.
# The .env file is ignored by git, so your secrets will not be committed.

# Database configuration
DB_NAME=batcher
DB_USER=batcher
DB_PASSWORD=your_database_password

# JWT secret - should be a long, random string (at least 256 bits)
JWT_SECRET=your_jwt_secret

# Default user credentials
USERNAME=user
PASSWORD=password
